<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Opción 1: Permitir TODO el tráfico cleartext solo mientras desarrollas -->
    <!-- (Es lo más rápido y funciona 100% en tu tablet física) -->
    <base-config cleartextTrafficPermitted="true" />

    <!-- Opción 2: Si prefieres ser más específico y seguro, descomenta esto y comenta la línea de arriba -->
    <!--
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.1.96</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
    -->
</network-security-config>
